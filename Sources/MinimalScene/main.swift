import Foundation
import ManimAgenticInterface

@main
struct MinimalScene {
    static func main() {
        print("""
        Minimal MAI playground entry point.

        This W1 scaffold imports ManimAgenticInterface so SwiftPM resolves the
        consumer dependency shape. Add real scene code after W2/W3 land the
        setup and render machinery.
        """)
    }
}
