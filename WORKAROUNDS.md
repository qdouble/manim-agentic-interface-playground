# Workarounds Log

Use this file for cycle-agent notes about temporary consumer-side workarounds.
Keep entries factual and remove them once MAI fixes the underlying issue.

## Template

```markdown
### YYYY-MM-DD — Short title

- Symptom:
- Exact command or scene:
- Temporary workaround:
- Why this should not become permanent:
```

Setup/render proof now belongs to the checked-in playground route. Record only
temporary consumer-side workarounds that remain after running `./setup.sh` and
the minimal scene.
